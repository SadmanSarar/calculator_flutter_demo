class CalculatedRowModel {
  String amount;
  int id;
  CalculatedRowModel(this.amount, this.id);

  @override
  String toString() {
    return "CalculatedRowModel( id: $id, amount: $amount);";
  }
}

